.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.DS_Store

# CIFAR-10, downloaded by torchvision on the first run. 340 MB, public, and
# regenerated by `make search`, so there is no reason to version it.
data/

# results/ IS tracked. The log and the figures are the result.

# Host build products. firmware/generated/ IS tracked: the op table is what CI
# diffs to catch a stale export, and the golden vectors are the test fixture.
firmware/test_equivalence
firmware/bench
build/

# compiled verifiers
verify/gocheck/gocheck
verify/check
